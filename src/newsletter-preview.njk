---
title: Weekly Newsletter Preview
layout: base.njk
permalink: /newsletter-preview/
---

<h1>Newsletter Preview</h1>

<h2>🧠 Links</h2>
<ul>
{% for post in collections.weekliesSorted %}
  {% if post.date and post.date >= lastSunday %}
    <li>
      {{ post.templateContent | safe }}
    </li>
  {% endif %}
{% endfor %}
</ul>

<h2>📖 Currently Reading</h2>
<ul>
{% for book in collections.allPendingBooks %}
  <li><strong>{{ book.data.title }}</strong> by {{ book.data.author }}</li>
{% endfor %}
</ul>

<h2>📚 Recent Book Commentary </h2>
<ul>
{% for book in collections.allBooksSorted %}
  {% if book.data.date_posted >= lastSunday %}
      <h3>{{ book.data.title }}</strong> by {{ book.data.author }}</a></h3>
      {{ book.templateContent | safe }}
  {% endif %}
{% endfor %}
</ul>

